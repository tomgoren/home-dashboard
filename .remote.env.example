# Copy to .remote.env and fill in your target device. Used by the
# `remote:*` mise tasks to provision/deploy over SSH from this machine,
# without needing to log into the device yourself first.

REMOTE_HOST=pi@10.0.0.37

# Optional overrides — defaults shown.
# REMOTE_DIR=~/home-dashboard
# REPO_URL=https://github.com/tomgoren/home-dashboard.git
